name: Publish OpenAPI schema page to GitHub pages site
on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read

env:
  SWAGGER_PATH: '../../docs/openapi.yml'

jobs:
  publish:
    runs-on: ubuntu-18.04
    steps:
      - run: echo "Publish step"